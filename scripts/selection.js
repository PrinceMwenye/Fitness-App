//this function takes a url and directs the webpage to the given url
function goTo(pageURL) {
    window.location.href = pageURL
}

//this function accpets an element and a color as a string and changes the color of the element accordingly
function changeBackgroundColor(element, color) {
    element.style.background = color;
}